# 🩺 تقرير شمولي — اصطياد وإصلاح أخطاء 500 (2026-09-15)
#audit #500 #bughunt #app-review-safe

> **المطلوب**: فحص شامل للموقع والكود والروابط يكشف 500، يجد سببه، يصلحه، ويوثَّق كل ذلك. **صفر تغييرات في الواجهة** (المراجعة جارية).

---

## 1) المنهجية — 3 طبقات اصطاد

| الطبقة | الأداة | النطاق |
|---|---|---|
| **Runtime edge sweep** | `audit_500_hunter.py` (44 حالة) | ويبهوكات مُوَقّعة بحمولات معادية (جسم غير JSON، list مكان dict، entry نص، value=null، messaging بغير sender) · مسارات بمعرّفات UUID فاسدة على 15 نقطة · دوال POST/PATCH/DELETE بأجسام مشوّهة · بارامترات استعلام معادية |
| **Static lint** | ruff (F821/F811) — جولة سابقة | كل NameError-class في الكود |
| **Live click matrix** | `probe_buttons_live.py` — جولة سابقة | 65 زرًا حيًا + كل روابط `<a href>` |

## 2) ما وُجد — 10 أصناف حقيقية من 500 — اتصلحت كلها

| # | العيب | الجذر الجذمى | الإصلاح |
|---|---|---|---|
| 1 | ويبهوك ميتا: جسم غير JSON (حتى بتوقيع صحيح) = 500 | `request.json()` بلا حماية → JSONDecodeError | `try/except → 400 "Malformed webhook JSON body"` |
| 2 | ويبهوك ميتا: payload = list = 500 | `.get` على list في parse | فحص isinstance → `ignored` بليق |
| 3 | ويبهوك ميتا: entry = string = 500 | التكرار على نص يحرفه حرفًا حرفًا ثم `.get` | isinstance في parse_messaging/parse_comment |
| 4 | ويبهوك ميتا: mentions value=null = 500 | `val.get` على None | حارس `value` dict في الفرعين feed/mentions |
| 5 | ويبهوك ثريدز: جسم غير JSON (مُوَقّع) = 500 | نفس الجذر | نفس الـ 400 |
| 6 | ويبهوك ثريدز: value نص = 500 | تكرار حارس الحلقة | isinstance كامل في حلقة الـ receiver |
| 7-14 | أي UUID فاسد في المسار = 500 (`/api/leads/x`, `/api/content/posts/x`, takeover, send-message, credits, plan, notifications/{id}/read, ai providers/models, identity queue, coupons) | PostgREST يرفض uuid غير صالح → DatabaseConnectionError يطفو 500 | **وسيط مركزي `uuid_segment_guard`** (regex لكل الأنماط) يُرجع 404 صادقًا — **مسجّل داخل طبقة التصديق** حتى يحافظ على 403 لغير الأدمن (اكتُشف وأُصلح حين كسر اختبار الأمان وأعيد ترتيبه) |

## 3) إيجابيات سليمة (لم نغيّر فيها شيئًا)

- كل حالات الإدخال الخاطئ المهيكلة (نوع enum خاطئ، body ناقص، array مكان object) = **422 نظيفة** من FastAPI ✅
- كل بارامترات الاستعلام الفوضوية = 200 مع نتائج فارغة صادقة ✅
- مدفوعات Polar ويبهوكها: fail-closed على مفتاح ناقص ✅
- معرفات غير uuid في مسارات نصية (filenames, wf ids, thread ids) لم تتأثر ✅

## 4) الأدلة

- **محليًا**: 44/44 حالة صفر 5xx · **317/317 اختبار** (منها 6 regressions جديدة `test_500_hunter.py` + اختبارات سابقة للتصحيح)
- **الإنتاج الحي** (`3d99b51`): signed garbage → **400** · `/api/leads/not-a-uuid` → **401** (البوابة أولًا) ✅
- السويتمان الأخيران السابقان (lint + الأزرار الحي) لا يزالان نظيفين

## 5) ما يمكن تشغيله للتكرار

```bash
python audit_500_hunter.py     # مصفوفة 44 حالة حدّية
python probe_buttons_live.py   # نقر حي لكل الأزرار الآمنة
ruff check src/ --select F821,F811
```
