# 📋 التقرير الشامل — تدقيق الموقع الكامل (2026-09-11)
#audit #p0-fixes #supabase #ai #threads #instagram #permissions

> **الأمر**: دراسة Brain/Memory/SOPs أولًا → فحص شامل لكل النقاط اللي طلبها المالك → إصلاح فوري عند الاكتشاف → نشر مباشر. التنفيذ في كوميت `fbb7aa1`.

---

## ✅ ملخص تنفيذي
26 صفحة فُحصت حيًا (× لغتين) · مصفوفة صلاحيات 36/36 · 3 migrations مطبقة · **5 باجات P0 حقيقية اتصلحت وأثبتت حيًا** · أخطاء قديمة ظهرت إنها artifacts اختبارية مش باجات.

---

## 🔴 P0 — باجات حقيقية اتصلمت واتصلحت (كلها مُتحقق منها حيًا)

### 1. باج Instagram views=0 (شكوى المالك الأصلية)
- **الجذر**: `feed_sync.fetch_instagram_media` كان فيه `"views_count": 0` مكتوبة يدويًا.
- **الإصلاح**: جلب حقيقي من Insights API الرسمي (`metric=views` مع fallback `plays`) لكل ريلز/فيديو، rate-limited، صفر اختلاق.
- **الإثبات الحي**: 19/25 ريلز جابوا مشاهدات حقيقية (58/29/25...) — الباقي تخطاه حماية الـ rate limiter ويتعبى في المزامنة التالية.
- **ملاحظة للايكات**: الـ API بيرجع 0 للايكات على كل الريلز — إما إخفاء اللايكات مفعّل في إعدادات الحساب أو توكن IG المخصص منتهي (زر التحديث في /settings). Views اشتغلت بتوكن الصفحة.

### 2. النشر على Threads كان ميت تمامًا
- **الجذر المزدوج**: `config.py` عمره ما حمّل `THREADS_ACCESS_TOKEN` من `.env` + الـ resolver بيقرأ من app_settings فقط.
- **الإصلاح**: إضافة الحقلين للإعدادات + env-fallback في `_get_stored_creds` (expires_at=0 = دائم).
- **الإثبات الحي**: publish → success → sync-replies → captured → delete → success (دورة كاملة).

### 3. أي Lead من Threads كان يفشل (enums ناقصة)
- **الجذر**: قاعدة البيانات بتستخدم Postgres ENUMs — `lead_source_enum` و `platform_enum` مفيهمش `threads`.
- **الإصلاح**: migrations 015 + 016 (016 بتصحح platform_enum اللي بيخدم 4 جداول).
- **الإثبات**: lead+message اتحفظوا بنجاح بعد التطبيق.

### 4. قاعدة معرفة الـ AI كانت **فاضية تمامًا** (أخطر اكتشاف)
- **الجذر المركب**: `_mode="db"` يقرأ من `kb_documents` (0 صف) ويرجع فورًا بلا fallback للملفات + **الرفع الرسمي نفسه بيكتب ملفات محلية فقط** (الجدول يتيم).
- **الإصلاح**: زرع 5 مستندات المالك الحقيقية عبر مسار الحفظ الرسمي (**بالـ embeddings** — 9 chunks) بـ `user_id` المالك + **إصلاح مسار الرفع** ليخزن في `kb_documents` باسم صاحب الجلسة.
- **الإثبات**: reload يحمّل 5 → RAG retrieval يرجّع chunks فيها التسعير → get_combined_context = 6534 حرف.
- ⚠️ `brand_tone.md` و `faqs.md` (المذكورين في SOP-09 كركائز) **غير موجودين أصلًا** — المالك يعدهم ويرفعهم.

### 5. كسر فصل الأدوار (باج الـ init المكرر)
- **الجذر**: تعريفين لـ `init()` في `hudhudRoleManager` — القديم (آخر خاصية بتفوز) كان بيلغي فحص الصلاحية والـ redirect والـ strip بالكامل. اكتُشف بـ fetch-interception داخل الصفحة.
- **الإصلاح**: حذف القديم + `DEV_ROUTES` توسعت لـ `/users`,`/templates` + `.client-nav-section` تُخفى في وضع Developer (فصل ثنائي الاتجاه حقيقي) + `/users`,`/templates` ربطوا بـ saas.css (كانوا بدون تنسيق).
- **الإثبات**: test_separation_live 5/5 (client→redirect، إخفاء متبادل، تنسيق سليم).

## 🟡 P1 — اكتشافات معمارية (موثقة لـ Wave 9.8 المخطط — مش اختراع)
1. **`automations_workflows` الجدول كان مش موجود أصلًا** في القاعدة الحية (الخدمة بتخزن في JSON ملف عام) → migration 014 أنشأته **جاهز لكل مستخدم** (user_id + RLS). قطع الخدمة من الملف للـ DB = Wave 9.8.
2. **كل بيانات المالك حاليًا `user_id=NULL`** (content_posts 64 صف، الرسائل...) — الـ per-user rewiring الشامل (webhook/cron/agent → توكن لكل مستخدم) هو Wave 9.8 المسجل.
3. **`/api/knowledge/documents` و `/api/content/posts` بترجع 200 لليوزر العادي** — سليمة مؤقتًا (بيانات المالك هي الوحيدة الموجودة) لكن Wave 9.8 لازم يفلترها بـ user_id.

## 🟢 الفحوصات اللي طلعت **نظيفة**
| الفحص | النتيجة |
|---|---|
| 26 صفحة × EN/AR حيًا (أخطاء JS + failed requests + KPI placeholders) | نظيف 100% — المشكلة الوحيدة أثر تنقل اختباري موثق |
| مصفوفة الصلاحيات (anon/user/admin × 12 صفحة + 7 APIs) | 36/36 |
| Redirects: كل الصفحات المحمية بـ `?next=` محفوظ | 11/11 |
| RLS مفعّل على 25/25 جدول + سياسات service-role | سليم |
| FKs والعلاقات (22 علاقة موثقة بالتقرير) | سليمة |
| مزود الـ AI (Google AI — 33 موديل مفعّل) | شغال |
| Settings tabs / Dashboard KPIs | شغالين (الفحص الحي) |
| .env مفاتيح مكررة | لا يوجد |

## ⚠️ بنود مفتوحة للقرار
1. **Wave 9.8** (مسجل من قبل): إعادة توصيل per-user للـ KB/automations/content + Threads webhook receiver — اكتشافات النهارده بقت متطلباته الجاهزة.
2. `PHASE_10_PLAN.md` فيه تلف ترميز قديم (cp1252) — يحتاج إصلاح جراحي.
3. تجديد توكن IG المخصص (زر /settings) لو المالك عايز أرقام لايكات غير مخفية.
4. `deploy_marker` في /health لسه قديم — يتحرك مع كل نشر (تحسين مقترح).

## 📦 الأثر
- **3 كوميتات**: `fbb7aa1` (التدقيق+الإصلاحات) · `e19ace4` (الفصل+i18n+/users) · `96a116b` (i18n) — كلها منشورة ومتحقق منها حيًا.
- **3 migrations**: 014 (automations table) · 015/016 (enums) — مطبقة على القاعدة الحية.
- **سكربتات فحص دائمة** قابلة لإعادة التشغيل: `audit_static/live/supabase/permissions/enums` + `verify_deploy`.
